# DAPF-LDM: Diagnosis-Aware Latent Diffusion for Pathology-Focused MRI-to-PET Synthesis

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Framework: PyTorch](https://img.shields.io/badge/Framework-PyTorch-orange.svg)](https://pytorch.org/)

This repository contains the official PyTorch implementation of the paper **DAPF-LDM: Diagnosis-Aware Latent Diffusion for Pathology-Focused MRI-to-PET Synthesis**.

## Table of Contents

1. [Model Overview](#1-model-overview)
2. [Setup and Dependencies](#2-setup-and-dependencies)
3. [Running the Model](#3-running-the-model)
4. [Results](#4-results)
5. [Contact](#5-contact)
6. [License](#6-license)

## 1. Model Overview

The training pipeline of our proposed model is shown below:

<div align="center">
  <img src="https://raw.githubusercontent.com/papercodeHua/DAPF_LDM/main/result/exp_1/fig1.png" width="95%">
</div>

MRI-to-PET synthesis often fails to capture disease-specific hypometabolism patterns, limiting its clinical utility for Alzheimer's disease (AD) diagnosis. To address this, we propose **DAPF-LDM**, a Diagnosis-Aware Latent Diffusion Model.

Operating in a compressed 3D latent space, our framework introduces a **Diagnosis-Aware Pathology-Focused (DAPF) block**. This block uses orthogonal decomposition to disentangle anatomical cues from noise and integrates anatomical priors with MMD-based adaptive weighting to explicitly emphasize AD-critical regions. Additionally, to bridge the semantic gap between modalities, we introduce a Cross-Modal Fusion-Perceptual Loss.

## 2. Setup and Dependencies

### Dataset

To train or test the model, you must first download the **ADNI** dataset from [here](https://adni.loni.usc.edu/data-samples/access-data/).

### Dependencies

Requirements: Python 3.8+, PyTorch 1.12+ (CUDA recommended).

```bash
pip install -r requirements.txt
```

### Directory Structure

```plaintext
├── datas                  # Data root directory
│   ├── atlas_masks        # Binary masks for 22 ROIs (generated by utils/split_atlas.py)
│   └── FDG
│       ├── MRI            # Source domain: T1-weighted MRI volumes (NIfTI)
│       │   ├── train
│       │   ├── val
│       │   ├── test
│       │   └── FDG_train_MRI_info.csv  # Metadata (Image_ID, Subject, Group, Age)
│       └── PET            # Target domain: Registered FDG-PET volumes (NIfTI)
│           ├── train
│           ├── val
│           ├── test
│           └── latent_FDG     # Generated by AAE encoding
├── model                  # Model definitions
│   ├── aae.py             # 3D Adversarial Autoencoder
│   ├── da_net.py          # Diagnosis-Aware Network (Guidance)
│   ├── dapf_block.py      # Core DAPF module
│   ├── unet.py            # Denoising UNet backbone
│   └── ...                
├── scheduler              # Diffusion scheduling modules
│   └── linear_scheduler.py  # Defines noise schedules and DDIM sampling logic
├── result                 # Checkpoints and logs
│   ├── exp_1              # Stage I: AAE results
│   ├── exp_da             # Stage I: DA-Net results
│   ├── exp_mri_pet        # Stage I: ResCNN results
│   └── exp_2              # Stage II: DAPF-LDM results (main)
├── utils                  # Utility functions
│   ├── config.py          # Hyperparameters and paths
│   ├── utils.py
│   └── split_atlas.py     # Tool to process Harvard-Oxford atlas
├── train_aae.py           # AAE training script
├── train_da_net.py        # Guidance Network training script
├── train_mulrescnn.py     # Perceptual Network training script
├── train_dapf_ldm.py      # Main LDM training script
├── test_dapf_ldm.py       # Inference and evaluation script
└── README.md
```

## 3. Running the Model

Configurations (batch size, learning rate, paths, etc.) can be modified in `utils/config.py`. The training process consists of two main stages.

### 3.0 Preprocessing

Generate ROI binary masks from the Harvard-Oxford atlas:

```bash
python utils/split_atlas.py --input ./datas/HarvardOxford-sub-maxprob-thr0-1mm_aligned.nii.gz --output ./datas/atlas_masks/
```

### 3.1 Stage I: Auxiliary Component Pre-training

Pre-train the auxiliary components before the main diffusion model.

1. Train 3D AAE (latent representation)

   ```bash
   # Set phase="train" in utils/config.py
   torchrun --nproc_per_node=num_gpus train_aae.py  # Adjust GPU count as needed
   ```

2. Encode PET data to latent space

   ```bash
   # Set phase="encoding" in utils/config.py
   torchrun --nproc_per_node=num_gpus train_aae.py
   ```

3. Train Diagnosis-Aware Network (DA-Net)

   ```bash
   torchrun --nproc_per_node=num_gpus train_da_net.py
   ```

4. Train Perceptual Loss Network (MultiModal ResCNN)

   ```bash
   torchrun --nproc_per_node=num_gpus train_mulrescnn.py
   ```

### 3.2 Stage II: DAPF-LDM Training

Train the main latent diffusion model (with DAPF block and Fusion Loss):

```bash
torchrun --nproc_per_node=num_gpus train_dapf_ldm.py  # Recommended: 2+ GPUs
```

### 3.3 Inference & Evaluation

Generate synthetic PET images on the test set and compute metrics (global: PSNR/SSIM; lesion-specific: ROI-MAE):

```bash
torchrun --nproc_per_node=num_gpus test_dapf_ldm.py
```

Results will be saved as CSV files in the corresponding experiment folder (e.g., `result/exp_2/test.csv`).

## 4. Results

Qualitative comparison with competing methods:

<div align="center">
  <img src="https://raw.githubusercontent.com/papercodeHua/DAPF_LDM/main/assets/results.png" width="95%">
</div>

DAPF-LDM (ours) effectively avoids anatomical hallucinations, preserves patient-specific topology, and accurately renders hypometabolism in critical regions (e.g., Precuneus).

## 5. Contact

If you find this work useful, please cite the paper:

```bibtex
@article{YourName202X_DAPF_LDM,
  title={DAPF-LDM: Diagnosis-Aware Latent Diffusion for Pathology-Focused MRI-to-PET Synthesis},
  author={Your Name and Co-authors},
  journal={IEEE Transactions on Medical Imaging},
  year={202X}
}
```

For questions about the code, feel free to open an issue or email: your_email@example.com

## 6. License

This repository is licensed under the MIT License.
```
